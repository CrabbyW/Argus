{
  "//": "Copy to appsettings.Development.json and fill in. appsettings*.json are gitignored per CLAUDE-dotnet.md.",

  "//ConnectionStrings": [
    "The roadplan asks for SQL Server authentication, which is the form below: a SQL login, not",
    "the Windows account. It needs a real SQL Server (Developer edition or a container) — point",
    "Server= at it and create the login there.",
    "",
    "The LocalDB fallback is kept for a machine with no SQL Server installed. LocalDB supports no",
    "SQL authentication at all, only the Windows account that owns the instance, so it cannot",
    "satisfy the roadplan on this point:",
    "  Server=(localdb)\\MSSQLLocalDB;Database=Argus;Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=True;",
    "",
    "Either way the password does not belong in this file once the database is not local — set",
    "ConnectionStrings__ArgusDatabase as an environment variable instead; it overrides this."
  ],
  "ConnectionStrings": {
    "ArgusDatabase": "Server=localhost,1433;Database=Argus;User Id=argus;Password=CHANGE_ME;TrustServerCertificate=True;MultipleActiveResultSets=True;"
  },
  "Jwt": {
    "Issuer": "Argus",
    "Audience": "ArgusClients",
    "SigningKey": "CHANGE_ME_TO_A_RANDOM_STRING_OF_AT_LEAST_32_CHARACTERS",
    "TokenLifetimeMinutes": 480
  },
  "Cors": {
    "AllowedOrigins": [ "http://localhost:4200" ]
  },
  "Database": {
    "MigrateAndSeedOnStartup": true
  },
  "//Seed": [
    "InstallationCount is how many installations the demo grid should hold. The seeder only",
    "tops the table up to it and never deletes, so 0 leaves just the hand-written demo rows."
  ],
  "Seed": {
    "AdminPassword": "CHANGE_ME",
    "InstallationCount": 200
  },
  "//AuditLog": [
    "The action log: one line per API request in logs/argus-actions.log, formatted",
    "[timestamp] [action] [command] [status], where command is the request as it was sent.",
    "RetentionDays deletes anything older; set it to 0 to keep every file forever."
  ],
  "AuditLog": {
    "Directory": "logs",
    "RetentionDays": 30,
    "SweepIntervalHours": 24
  },
  "//Logging": [
    "Program.cs routes Microsoft.Extensions.Logging into log4net, so these levels decide what",
    "the framework contributes to logs/argus-api.log — and therefore to the /logs screen.",
    "",
    "Database.Command is the SQL EF Core sends to the server, one entry per command, at",
    "Information. Set it to Warning to keep it out of the log; the rest of the file is easier to",
    "read without it, and on a busy day it is most of the volume.",
    "",
    "Parameter values are logged as '?', never the value itself. They stay hidden unless someone",
    "calls EnableSensitiveDataLogging() on the DbContext, which nothing here does and which has no",
    "place in a log this application will happily serve over its own API."
  ],
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information",
      "Microsoft.EntityFrameworkCore.Infrastructure": "Warning",
      "Microsoft.EntityFrameworkCore.Migrations": "Information"
    }
  },
  "AllowedHosts": "*"
}
